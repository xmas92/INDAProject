package game.Controller;

public interface Controller {
	/**
	 * Updates and handles the control of the object
	 * @param delta time since last call to Update(int);
	 */
	void Update(int delta);
}
